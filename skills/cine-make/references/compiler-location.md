# Cine Make Compiler Location

This file is overwritten by `cine-make install-skill`.

Until installed, locate the compiler by finding the directory that contains `src/cli.mjs` and `package.json` with package name `cine-make`.

